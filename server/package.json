{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Taylor Swanson",
  "scripts": {
    "build-ts": "rm -rf ./dist && npx tsc",
    "dev": "npm run build-ts && NODE_ENV=dev node --trace-warnings ./dist",
    "start": "NODE_ENV=production node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "socket.io": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.25",
    "source-map-support": "^0.5.21",
    "typescript": "^4.6.3"
  }
}
