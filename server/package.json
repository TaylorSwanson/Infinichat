{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rm -rf ./dist && npx tsc && NODE_ENV=dev node --trace-warnings ./dist",
    "start": "NODE_ENV=production node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taylor Swanson",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.25",
    "typescript": "^4.6.3"
  }
}
